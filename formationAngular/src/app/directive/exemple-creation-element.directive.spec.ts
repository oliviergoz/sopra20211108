import { ExempleCreationElementDirective } from './exemple-creation-element.directive';

describe('ExempleCreationElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ExempleCreationElementDirective();
    expect(directive).toBeTruthy();
  });
});
